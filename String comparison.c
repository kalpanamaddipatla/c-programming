# c-programming
#include <stdio.h>
#include<string.h>
int main() 
{
   char str1[10]="HELLO";
   char str2[10]="HEY";
   if(strncmp(,str2,2)==0)
    printf("\nThe two strings are identical");
    else
    printf("\n The two strings are not identical");
    return 0;
}
